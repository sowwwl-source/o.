# o-sshca infra

This folder contains OpenSSH bastion configuration snippets and scripts to generate the CA keys and (optionally) a KRL.

Nothing in `infra/ca/` should be committed (private CA key).

