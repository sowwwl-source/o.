# Canonical host used by client-side redirect on *.sowwwl.cloud.
VITE_CANONICAL_HOST=0.user.o.sowwwl.cloud

# Dev-only: backend API target used by Vite proxy (/api -> target).
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
