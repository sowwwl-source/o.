# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Local backups (scripts/db-manager.sh)
backups/

# OS junk
.DS_Store

# Node / bundlers
**/node_modules/
**/dist/
**/.next/
**/*.tsbuildinfo

# SSH CA / local state (new system)
backend/data/
infra/ca/
infra/bastion/revoked.krl
infra/bastion/revoked.krl-spec
